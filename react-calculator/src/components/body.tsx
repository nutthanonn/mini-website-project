import React from "react";
import Box from "@mui/material/Box";

import { observer } from "mobx-react";

const Body: React.FC = () => {
  return (
    <Box>
      <p></p>
    </Box>
  );
};

export default Body;
